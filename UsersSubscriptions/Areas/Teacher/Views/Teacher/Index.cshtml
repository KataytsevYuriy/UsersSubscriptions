
@{
    Layout = "_TeacherLayout";
}
<script src="~/js/qr_packed.js"></script>
@*<script src="https://rawgit.com/sitepoint-editors/jsqrcode/master/src/qr_packed.js"></script>*@
<script>
    function openQRCamera(node) {
        var reader = new FileReader();
        reader.onload = function () {
            node.value = "";
            qrcode.callback = function (res) {
                if (res instanceof Error) {
                    alert("QR не відскановано. Будьласка наведіть камеру на QR коду, та спробуйте ще.");
                } else {
                    node.parentNode.previousElementSibling.value = res;
                }
            };
            qrcode.decode(reader.result);
        };
        reader.readAsDataURL(node.files[0]);
        $("#StudentInfo").click();
    }
</script>
<form asp-action="StudentInfo" method="post">
    <input type=text size=16 placeholder="QR код" class=qrcode-text name="studentQR">

    <label class=qrcode-text-btn>
        <input type=file accept="image/*" onclick="return showQRIntro();" onchange="openQRCamera(this);" tabindex=-1>
    </label>
    <button type="submit" id="StudentInfo" class="btn btn-primary">Далі</button>
</form>